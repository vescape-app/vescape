output:
  - failure

post-merge:
  commands:
    install:
      glob: 'bun.lock'
      run: bun install

pre-commit:
  parallel: true
  commands:
    format:
      glob: '*.{ts,tsx,json,md,yml,yaml}'
      run: bunx oxfmt {staged_files}
      stage_fixed: true
    skills-check:
      glob: '{.agents,.claude}/skills/**'
      run: bun run skills-check
      stage_fixed: true
    typecheck:
      glob: '*.{ts,tsx}'
      run: bun run ts
    lint:
      glob: '*.{ts,tsx,json}'
      run: bun run lint
    knip:
      glob: '*.{ts,tsx,json}'
      run: bun knip
    test-bun:
      glob: '*.{ts,tsx}'
      run: bun run test:bun
    test-android:
      glob: '*.{kt,kts}'
      run: bun run test:android

commit-msg:
  commands:
    commit-policy:
      run: bun run scripts/git-hooks/policy.ts commit-msg "{1}"

pre-push:
  commands:
    commit-policy:
      run: bun run scripts/git-hooks/policy.ts pre-push
      use_stdin: true
