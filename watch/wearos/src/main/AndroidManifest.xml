<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-feature android:name="android.hardware.type.watch"/>
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
  <uses-permission android:name="android.permission.WAKE_LOCK"/>
  <!-- Radar imagery is the one thing the wrist fetches itself; everything else comes from the phone. -->
  <uses-permission android:name="android.permission.INTERNET"/>

  <application
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:label="Vescape"
    android:supportsRtl="true"
    android:theme="@style/Theme.Vescape.Splash">

    <uses-library android:name="com.google.android.wearable" android:required="true"/>
    <!-- Depends on the phone for telemetry, so it is not a standalone watch app. -->
    <meta-data android:name="com.google.android.wearable.standalone" android:value="false"/>

    <activity
      android:name=".MainActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
      <!-- Phone-initiated launch on board connect: RemoteActivityHelper requires VIEW + BROWSABLE
           with a data URI. Must match WATCH_MIRROR_URI in the phone-side WatchMirrorLauncher. -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW"/>
        <category android:name="android.intent.category.DEFAULT"/>
        <category android:name="android.intent.category.BROWSABLE"/>
        <data android:scheme="vescape" android:host="mirror"/>
      </intent-filter>
    </activity>
  </application>
</manifest>
